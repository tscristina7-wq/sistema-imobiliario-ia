import sqlite3
from flask import g

DATABASE = "sistema_imobiliario.db"

def get_db():
    if "db" not in g:
        g.db = sqlite3.connect(DATABASE)
        g.db.row_factory = sqlite3.Row
    return g.db
